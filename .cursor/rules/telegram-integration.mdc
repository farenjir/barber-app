---
description: Telegram bot integration rules
globs:
  - "src/app/api/webhooks/telegram/**"
  - "src/lib/messages.ts"
---

## Raw Telegram Bot API Only

- Use raw Bot API webhook at `src/app/api/webhooks/telegram/route.ts`
- Direct `fetch` to `https://api.telegram.org/bot{token}/...`
- Message templates in `src/lib/messages.ts`

## Forbidden

- ❌ Do NOT reintroduce Vercel Chat SDK
- ❌ Do NOT add `@chat-adapter/telegram`
- ❌ Do NOT import from `@vercel/ai`

## Customer Access

- Customers book via Telegram bot OR web at `/book/<code>` with barber's web invite link
- Customers can access their appointments on web at `/customer` via magic link from Telegram bot (ورود به وب)
- Barbers and admins use web panels
- Telegram customers access barber via `/start` parameter with barber code
